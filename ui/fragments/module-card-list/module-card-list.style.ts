import { StyleSheet } from "react-native";

export const moduleCardListStyles = StyleSheet.create({
  container: {
    paddingTop: 48,
    paddingHorizontal: 16,
    paddingBottom: 32,

    gap: 16,
  },
});
